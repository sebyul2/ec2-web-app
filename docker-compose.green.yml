version: '2.1'
services:
  app:
    environment:
     - NODE_ENV=development
     - PORT=4001
    command: npm start
    build: 
      context: ./
      dockerfile: Dockerfile-app
  nginx:
    command: nginx
    build: 
      context: ./
      dockerfile: Dockerfile-nginx
    links:
      - app
    ports:
      - "80:81"
    depends_on:
      app:
        condition: service_started
    volumes:
      -  .:/data/web-app