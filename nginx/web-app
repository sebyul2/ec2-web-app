upstream web-app {
    server app-blue:4000 max_fails=5 fail_timeout=30s;
    server app-green:4001 max_fails=5 fail_timeout=30s;
}

server {
    listen      80;
    listen      81;
    server_name _;

    location / {
        expires -1;
        proxy_pass http://web-app;
    }
}